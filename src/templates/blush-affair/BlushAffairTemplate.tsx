// Blush Affair Template - Engagement, rose/floral theme
// Focus on couple story and engagement details
// Sections: Hero → Couple Story → Engagement Date → Gallery → RSVP → Footer

import CrimsonShaadiTemplate from '../crimson-shaadi/CrimsonShaadiTemplate';
import { TemplateProps } from '@/templates/types';

const BlushAffairTemplate = (props: TemplateProps) => {
  // For now, use the same component with blush theme applied via data-theme
  return <CrimsonShaadiTemplate {...props} />;
};

export default BlushAffairTemplate;
